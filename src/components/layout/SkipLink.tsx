/** Spec §30 — keyboard users can skip the header/nav straight to main content. */
export function SkipLink() {
  return (
    <a href="#main-content" className="skip-link">
      Skip to main content
    </a>
  );
}
